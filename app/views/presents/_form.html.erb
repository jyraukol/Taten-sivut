<%= form_for @present do |f| %>
  <% if @present.errors.any? %>
  <div class="error_messages">
    <h2>Lomakkeen tiedoissa oli virheitä:</h2>
    <ul>
      <% for message in @present.errrors.full_messages %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
<p>
<%= f.label :nimi %><br />
<%= f.text_field :name %>
</p>
<p>
<%= f.radio_button(:reserved, true) %>
<%= f.label(:reserved_true, "Varattu") %>
<%= f.radio_button(:reserved, false) %>
<%= f.label(:reserved_false, "Vapaa") %>
</p>
<p>
  <%= f.label :varaaja %><br />
  <%= f.text_field :reserver %>
</p>
<p class="button"><%= f.submit %></p>
<% end %>
