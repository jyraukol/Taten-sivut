<h1>Presents</h1>

<ul>
  <% for present in @presents %>
    <li
    <% if present.reserved == true %>
      class="reserved"
  <% else %>
  <% end %>  
  >
  <%= link_to present.name, reserve_path(:present => present.id) %></li>
  <ul> <li><%= link_to "Edit", edit_present_path(present) %> 
     <a href="link">Destroy </a></li>
  </ul>
<% end %>
</ul>
